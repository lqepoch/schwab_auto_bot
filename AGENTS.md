# 运行优先交付顺序

对于用户明确授权的时间敏感运行时变更，先以受限参数启动或切换服务，并确认进程唯一、运行锁、审计日志和目标行为正常；确认运行无误后，再补充测试、文档、提交、PR 和合并到 `main`。

此顺序不允许绕过既有安全边界：真实 broker 写入仍须显式 live 确认、单一串行最终写入、风险限额、未知写入结果的只读对账，以及可审计日志。新 Submit 必须先经 Schwab Preview；对当前权威订单快照中已确认仍在工作的既有订单，精确调用原生 `PUT /orders/{orderId}` Replace 且保持策略身份不变时，不重复 Preview，直接按 Replace 语义进入最终写入。Replace 源订单不存在、已终态、端点不精确或策略身份变化时必须失败关闭。未获得用户明确授权时，不得启动、停止、重启或改变 live 交易服务。
