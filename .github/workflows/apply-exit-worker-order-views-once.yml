name: Apply Exit Worker Order Views Once

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/apply-exit-worker-order-views-once.yml'
      - 'scripts/patch-exit-worker-order-views.py'
      - 'src/automation/orderRuntimeIndex.ts'
      - 'src/automation/runtimeOrchestrator.ts'
      - 'test/order_runtime_index.test.ts'
      - 'test/runtime_exit_worker_order_views.test.ts'

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'perf/cache-exit-worker-order-views-20260818'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: perf/cache-exit-worker-order-views-20260818
          fetch-depth: 0

      - name: Detect pending patch
        id: pending
        shell: bash
        run: |
          if test -f scripts/patch-exit-worker-order-views.py; then
            echo 'apply=true' >> "$GITHUB_OUTPUT"
          else
            echo 'apply=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Node.js 24
        if: steps.pending.outputs.apply == 'true'
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install locked dependencies
        if: steps.pending.outputs.apply == 'true'
        run: npm ci --ignore-scripts

      - name: Apply exit-worker cache integration
        if: steps.pending.outputs.apply == 'true'
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/patch-exit-worker-order-views.py
          rm scripts/patch-exit-worker-order-views.py

      - name: Validate exit-worker cached order views
        if: steps.pending.outputs.apply == 'true'
        shell: bash
        run: |
          set -euo pipefail
          npm test
          npm run lint:deadcode
          pwsh -NoProfile -File ./test/hot_switch_identity.test.ps1
          mkdir -p .artifacts/runtime-benchmark
          for run in 1 2 3; do
            npm run benchmark:runtime -- --output ".artifacts/runtime-benchmark/run-${run}.json" >/dev/null
          done
          npm run benchmark:runtime:check -- --baseline bench/runtime-baseline.json --current-dir .artifacts/runtime-benchmark --min-samples 3
          rm -rf .artifacts/runtime-benchmark
          git diff --check
          grep -q 'for (const order of allActiveClosingOrders())' src/automation/runtimeOrchestrator.ts
          grep -q 'return activeClosingOrders(strategy).length > 0;' src/automation/runtimeOrchestrator.ts

      - name: Commit validated product changes
        if: steps.pending.outputs.apply == 'true'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git reset .github/workflows/apply-exit-worker-order-views-once.yml
          git commit -m 'perf: cache exit-worker closing-order views'
          git push origin HEAD:perf/cache-exit-worker-order-views-20260818
